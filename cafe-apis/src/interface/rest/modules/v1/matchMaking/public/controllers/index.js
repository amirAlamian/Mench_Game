const normalMatch = require('./normalMatch');
const getMatch = require('./getMatch');
const cancelMatch = require('./cancelMatch');

module.exports = {
    normalMatch,
    getMatch,
    cancelMatch,
};
